// Criar uma função que altere qualquer coisa

let tela = 'tela1';

function resetaTela(nTela, cor) {
    tela = nTela + ' ' + cor;
};

console.log(tela);

resetaTela('tela2', 'preta');

console.log(tela)